import AddClientSection from "@/components/addClientSection";

export default async function Dashboard() {
    return(
        <>
        <p> por si acaso</p>
        <AddClientSection />
        </>
    );
}